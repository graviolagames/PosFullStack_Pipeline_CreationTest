name: jobs examples

on:
  push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: build step
        run: echo Hello world ! Build.
  deploy:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: deploy step
        run: echo Hello world! Deploy.
